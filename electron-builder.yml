appId: "com.involvex.pixelforge"
productName: "PixelforgeAiStudio"
publish:
  - provider: "github"
artifactName: "PixelforgeAiStudio-${version}-${platform}-${arch}.${ext}"
asarUnpack:
  - "**/node_modules/**/*"
  - "dist/favicon.png"
  - "dist/favicon.ico"
compression: "maximum"
executableName: "PixelforgeAiStudio"
extraFiles:

files:
  - "dist/**/*"
  - "package.json"
  - "public/**/*"

  - "electron-builder.yml"

icon: "build/icons/icons/win/icon.ico"
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true

win:
  icon: "build/icons/icons/win/icon.ico"
  target:
    - target: "nsis"
      arch: "x64"
    - target: "portable"
      arch: "x64"
  verifyUpdateCodeSignature: false
directories:
  output: "release"
